/*global define: false */
/*jslint nomen: true */

define(['underscore',
        'backbone'], function (_, Backbone) {
    "use strict";
    var FriendModel = Backbone.Model.extend({
    });
    return FriendModel;
});